# 深度图 → 3D 点云清洗流水线 — 课堂 5 步法

> **课堂任务**：从杂乱的桌面深度图中，利用 SAM 掩膜提取目标物体，经过反投影、直通滤波、统计去噪、体素瘦身，得到干净可用的目标点云。
>
> **示例数据**：[`data/clutter_depth_demo/`](data/clutter_depth_demo/) — 合成桌面杂乱场景，包含中心目标盒体、左侧杯状物、右侧书本、后方小盒子
>
> **配套脚本**：[`pipeline_from_syllabus.py`](pipeline_from_syllabus.py) — 完全自包含，严格对应 5 步法

---

## 目录

1. [流水线总览](#1-流水线总览)
2. [环境与数据](#2-环境与数据)
3. [步骤 1 — 掩膜筛选](#3-步骤-1--掩膜筛选)
4. [步骤 2 — 反投影生成](#4-步骤-2--反投影生成)
5. [步骤 3 — 空间粗裁（直通滤波）](#5-步骤-3--空间粗裁直通滤波)
6. [步骤 4 — 精洗去噪](#6-步骤-4--精洗去噪)
7. [步骤 5 — 目标点云导出](#7-步骤-5--目标点云导出)
8. [一键运行](#8-一键运行)
9. [参数调优](#9-参数调优)
10. [常见问题](#10-常见问题)

---

## 1. 流水线总览

```
                        ┌──────────────┐
  深度图 (depth.png) ──▶│  步骤 1       │──▶ masked_depth
  掩膜   (mask.png)  ──▶│  掩膜筛选     │    (非目标置零)
                        └──────┬───────┘
                               ▼
                        ┌──────────────┐
                        │  步骤 2       │──▶ 3D 点云 (X, Y, Z)
                        │  反投影生成   │    create_from_depth_image()
                        └──────┬───────┘
                               ▼
                        ┌──────────────┐
                        │  步骤 3       │──▶  Z 轴裁剪后点云
                        │  直通滤波     │    numpy 切片
                        └──────┬───────┘
                               ▼
                        ┌──────────────┐
                        │  步骤 4       │──▶ 干净点云
                        │  精洗去噪     │    统计 → 体素
                        └──────┬───────┘
                               ▼
                        ┌──────────────┐
                        │  步骤 5       │──▶ target_clean.pcd
                        │  导出 .pcd    │    用于位姿估计
                        └──────────────┘
```

### 核心公式：针孔相机反投影

$$
Z = \frac{\text{depth}(u, v)}{\text{depth\_scale}}, \qquad
X = \frac{(u - c_x) \cdot Z}{f_x}, \qquad
Y = \frac{(v - c_y) \cdot Z}{f_y}
$$

---

## 2. 环境与数据

### 2.1 依赖安装

```bash
pip install numpy open3d Pillow
```

### 2.2 数据文件清单

所有输入文件位于 [`data/clutter_depth_demo/`](data/clutter_depth_demo/)：

| 文件 | 格式 | 说明 |
|------|------|------|
| [`depth.png`](data/clutter_depth_demo/depth.png) | 16-bit PNG | 深度图，640×480，单位毫米 |
| [`mask.png`](data/clutter_depth_demo/mask.png) | 8-bit PNG | 目标物体二值掩膜 |
| [`intrinsics.json`](data/clutter_depth_demo/intrinsics.json) | JSON | 相机内参 |
| [`rgb.png`](data/clutter_depth_demo/rgb.png) | 24-bit PNG | 对齐 RGB 预览图 |

**场景内容**：合成桌面杂乱场景，包含：
- **中心目标盒体**（掩膜标注的目标）
- 左侧杯状遮挡物
- 右侧书本状物体
- 后方小盒子
- 桌面深度渐变 + 传感器噪声

### 2.3 加载数据

```python
import numpy as np
import json
from PIL import Image
import open3d as o3d

# 深度图
depth_mm = np.asarray(Image.open("data/clutter_depth_demo/depth.png"))
print(f"深度图: {depth_mm.shape}, dtype={depth_mm.dtype}, "
      f"范围 [{depth_mm.min()}, {depth_mm.max()}] mm")

# 掩膜
mask = np.asarray(Image.open("data/clutter_depth_demo/mask.png").convert("L")) > 0
print(f"掩膜: {mask.shape}, 前景 {mask.sum():,}/{mask.size:,} ({mask.sum()/mask.size*100:.1f}%)")

# 内参
with open("data/clutter_depth_demo/intrinsics.json") as f:
    K = json.load(f)
print(f"内参: fx={K['fx']}, fy={K['fy']}, cx={K['cx']}, cy={K['cy']}, "
      f"depth_scale={K['depth_scale']}")
```

**预期输出**：
```
深度图: (480, 640), dtype=uint16, 范围 [0, 65535] mm
掩膜: (480, 640), 前景 2,392/307,200 (0.8%)
内参: fx=575.0, fy=575.0, cx=320.0, cy=240.0, depth_scale=1000.0
```

---

## 3. 步骤 1 — 掩膜筛选

### 3.1 原理

将深度图与 Mask 逐元素相乘，非目标像素深度值置为 0。后续反投影时只有目标区域生成 3D 点。

### 3.2 代码

```python
# 核心操作：一行乘法
masked_depth = depth_mm * mask

# 统计
print(f"掩膜前有效深度像素: {np.count_nonzero(depth_mm):,}")
print(f"掩膜后有效深度像素: {np.count_nonzero(masked_depth):,}")
```

### 3.3 对应函数

[`pipeline_from_syllabus.py` 第 81 行](pipeline_from_syllabus.py:81) — `step1_mask_filtering()`

### 3.4 本数据集结果

```
深度图总像素数:   307,200
掩膜前景像素数:   2,392 (0.8%)
掩膜前有效深度:   305,601
掩膜后有效深度:   1,951  (减少了 303,650)
```

> 掩膜内有效深度像素（1,951）小于掩膜前景像素（2,392），因为目标边缘部分像素在深度图中无有效值（传感器边缘空洞现象）。

---

## 4. 步骤 2 — 反投影生成

### 4.1 原理

使用 Open3D 内置函数 [`create_from_depth_image()`](https://www.open3d.org/docs/release/python_api/open3d.geometry.PointCloud.html#open3d.geometry.PointCloud.create_from_depth_image)，将掩膜筛选后的深度图映射为 3D 点云。该函数内部执行针孔相机反投影公式。

### 4.2 代码

```python
# 毫米 → 米
depth_m = masked_depth.astype(np.float32) / 1000.0

# 构造 Open3D 图像 + 内参对象
o3d_depth = o3d.geometry.Image(depth_m)
h, w = masked_depth.shape
intr = o3d.camera.PinholeCameraIntrinsic(w, h, K["fx"], K["fy"], K["cx"], K["cy"])

# 反投影
pcd = o3d.geometry.PointCloud.create_from_depth_image(
    depth=o3d_depth,
    intrinsic=intr,
    extrinsic=np.eye(4),   # 不旋转
    depth_scale=1.0,        # 已转米
    depth_trunc=100.0,      # 不截断
    stride=1,
)
print(f"生成 {len(pcd.points):,} 个 3D 点")
```

### 4.3 对应函数

[`pipeline_from_syllabus.py` 第 115 行](pipeline_from_syllabus.py:115) — `step2_backprojection()`

### 4.4 结果截图

![02-反投影原始点云](data/clutter_depth_demo/output/screenshots_syllabus/stage_00_02-反投影原始点云.png)

> 反投影后仅目标盒体生成了 3D 点，桌面和其他物体被掩膜排除。点数为 1,951。

---

## 5. 步骤 3 — 空间粗裁（直通滤波）

### 5.1 原理

用 numpy 布尔切片沿 Z 轴裁剪点云，移除：
- **Z < 0.2 m**：桌面以下的无效点
- **Z > 1.5 m**：距离过远的背景点

### 5.2 代码

```python
points = np.asarray(pcd.points)
z = points[:, 2]

# numpy 切片
z_mask = (z >= 0.20) & (z <= 1.50)

# 构造新点云
pcd_crop = o3d.geometry.PointCloud()
pcd_crop.points = o3d.utility.Vector3dVector(points[z_mask])
if pcd.has_colors():
    pcd_crop.colors = o3d.utility.Vector3dVector(np.asarray(pcd.colors)[z_mask])

print(f"滤波前: {len(points):,}")
print(f"滤波后: {len(pcd_crop.points):,}")
print(f"Z 范围: [{z.min():.3f}, {z.max():.3f}] m")
```

### 5.3 对应函数

[`pipeline_from_syllabus.py` 第 201 行](pipeline_from_syllabus.py:201) — `step3_passthrough()`

### 5.4 结果截图

![03-直通滤波后](data/clutter_depth_demo/output/screenshots_syllabus/stage_01_03-直通滤波后.png)

### 5.5 本数据集结果

```
滤波前: 1,951 点
滤波后: 1,951 点  (未剔除)
Z 范围: [1.297, 1.357] m  → 全部在 0.2~1.5m 内
```

> 本数据集的合成深度图中目标 Z 全部在有效范围内，因此点数不变。真实场景中此步骤会剔除桌面以下飞点和远处背景。

---

## 6. 步骤 4 — 精洗去噪

### 6.1 子步骤 (a)：统计滤波去噪

**原理**：对每个点计算到 k 个最近邻的平均距离，剔除平均距离过大的孤立飞点。

**代码**：

```python
pcd_stat, outlier_idx = pcd.remove_statistical_outlier(
    nb_neighbors=20,   # 邻居数
    std_ratio=2.0,     # 标准差倍数阈值
)
print(f"剔除飞点: {len(pcd.points) - len(pcd_stat):,}")
```

**结果截图**：

![04a-统计滤波后](data/clutter_depth_demo/output/screenshots_syllabus/stage_02_04a-统计滤波后.png)

**本数据集**：1,951 → 1,864（剔除 87 飞点，4.5%）

### 6.2 子步骤 (b)：体素降采样瘦身

**原理**：将 3D 空间划分为 `voxel_size` 边长的立方体网格，每个网格内只保留一个代表点。

**代码**：

```python
pcd_down = pcd_stat.voxel_down_sample(voxel_size=0.005)   # 5mm
print(f"降采样前: {len(pcd_stat.points):,}")
print(f"降采样后: {len(pcd_down.points):,}")
print(f"压缩比: {len(pcd_down.points)/len(pcd_stat.points)*100:.1f}%")
```

**结果截图**：

![04b-体素降采样后](data/clutter_depth_demo/output/screenshots_syllabus/stage_03_04b-体素降采样后.png)

**本数据集**：1,864 → 1,086（压缩至 58.3%）

### 6.3 对应函数

[`pipeline_from_syllabus.py` 第 254 行](pipeline_from_syllabus.py:254) — `step4_denoise_and_downsample()`

### 6.4 处理顺序讨论

| 顺序 | 本脚本默认 | 说明 |
|------|-----------|------|
| **先统计 → 后体素** | ✅ 推荐 | 先剔除飞点再降采样，几何信息保留更完整 |
| 先体素 → 后统计 | ❌ | 小簇飞点被体素合并后更难识别 |

---

## 7. 步骤 5 — 目标点云导出

### 7.1 代码

```python
output_path = "data/clutter_depth_demo/output/syllabus_target_clean.pcd"
os.makedirs(os.path.dirname(output_path), exist_ok=True)
o3d.io.write_point_cloud(output_path, pcd_down)

# 验证
pcd_check = o3d.io.read_point_cloud(output_path)
pts = np.asarray(pcd_check.points)
print(f"最终点数: {len(pts):,}")
print(f"X: [{pts[:,0].min():.3f}, {pts[:,0].max():.3f}]")
print(f"Y: [{pts[:,1].min():.3f}, {pts[:,1].max():.3f}]")
print(f"Z: [{pts[:,2].min():.3f}, {pts[:,2].max():.3f}]")
```

### 7.2 对应函数

[`pipeline_from_syllabus.py` 第 338 行](pipeline_from_syllabus.py:338) — `step5_export()`

### 7.3 本数据集最终输出

```
✅ 已保存: data/clutter_depth_demo/output/syllabus_target_clean.pcd
最终点数: 1,086

空间范围:
  X: [-0.190, -0.071] m
  Y: [0.229, 0.344] m
  Z: [1.303, 1.355] m
```

---

## 8. 一键运行

### 8.1 使用 [`pipeline_from_syllabus.py`](pipeline_from_syllabus.py)

```bash
# 不显示 3D 窗口（仅保存文件）
python pipeline_from_syllabus.py --demo-dir data/clutter_depth_demo --no-vis

# 显示最终 3D 结果
python pipeline_from_syllabus.py --demo-dir data/clutter_depth_demo

# 跳过截图（只输出 .pcd）
python pipeline_from_syllabus.py --demo-dir data/clutter_depth_demo --no-vis --skip-screenshots

# 自定义滤波参数
python pipeline_from_syllabus.py \
    --demo-dir data/clutter_depth_demo \
    --z-min 0.3 --z-max 1.2 \
    --voxel-size 0.008 \
    --nb-neighbors 30 --std-ratio 1.5 \
    --no-vis
```

### 8.2 输出产物

运行后生成到 `data/clutter_depth_demo/output/`：

| 路径 | 说明 |
|------|------|
| `syllabus_target_clean.pcd` | 最终干净目标点云 |
| `screenshots_syllabus/stage_00_02-反投影原始点云.png` | 步骤 2 截图 |
| `screenshots_syllabus/stage_01_03-直通滤波后.png` | 步骤 3 截图 |
| `screenshots_syllabus/stage_02_04a-统计滤波后.png` | 步骤 4a 截图 |
| `screenshots_syllabus/stage_03_04b-体素降采样后.png` | 步骤 4b 截图 |
| `screenshots_syllabus/comparison_all.png` | 四阶段并排对比图 |
| `screenshots_syllabus/montage_2x2.png` | 2×2 拼图 |

### 8.3 输出汇总

```
步骤 2（反投影）:       1,951 点
步骤 3（直通滤波）:     1,951 点     (Z 全部在 0.2-1.5m 内)
步骤 4a（统计滤波）:    1,864 点     (剔除 87 飞点)
步骤 4b（体素降采样）:  1,086 点     (压缩至 55.7%)
```

### 8.4 其他运行方式

```bash
# Open3D 独立教学脚本（使用斯坦福兔子点云）
python Open3D_PointCloud_Pipeline.py --no-vis

# 深度图+掩膜管道（输出场景/目标/清洗三个 .pcd）
python DepthMask_PointCloud_Pipeline.py --demo-dir data/clutter_depth_demo --no-vis

# 交互式模式（OpenCV 窗口 + SAM2 分割）
python interactive_depth_pipeline.py --source file \
    --input-dir data/clutter_depth_demo \
    --output-dir data/clutter_depth_demo
```

---

## 9. 参数调优

### 9.1 参数速查

| 参数 | 默认值 | 范围 | 说明 | 调大 | 调小 |
|------|--------|------|------|------|------|
| `--z-min` | 0.20 m | 0.0~1.0 | 直通 Z 最小值 | 剔除更多近点 | 保留更多近点 |
| `--z-max` | 1.50 m | 0.5~5.0 | 直通 Z 最大值 | 保留更远点 | 剔除远处点 |
| `--voxel-size` | 0.005 m | 0.002~0.020 | 体素尺寸 | 点更少、更快 | 更精细 |
| `--nb-neighbors` | 20 | 5~50 | 统计滤波邻居数 | 更宽松 | 更严格 |
| `--std-ratio` | 2.0 | 0.5~3.0 | 标准差阈值 | 更宽松 | 更严格 |

### 9.2 场景推荐

| 场景 | z_min | z_max | voxel_size | nb_neighbors | std_ratio |
|------|-------|-------|------------|--------------|-----------|
| 桌面小物体 | 0.20 | 1.00 | 0.005 | 20 | 2.0 |
| 桌面大物体 | 0.20 | 1.50 | 0.008 | 30 | 2.0 |
| 近距离抓取 | 0.10 | 0.80 | 0.003 | 15 | 1.5 |
| 高噪声传感器 | 0.15 | 1.20 | 0.008 | 10 | 1.0 |

---

## 10. 常见问题

### Q1：反投影后点云有空洞

**原因**：深度传感器在物体边缘、反光表面产生无效深度值。

**解决**：对掩膜做膨胀（dilate）覆盖边缘，或对深度图做中值滤波。

### Q2：统计滤波剔除了目标本身

**原因**：`std_ratio` 过小或 `nb_neighbors` 过大。

**解决**：增大 `std_ratio`（2.0→3.0），减小 `nb_neighbors`（20→10）。

### Q3：体素降采样后目标几何变形

**原因**：`voxel_size` 过大，网格太粗。

**解决**：抓取任务建议 `voxel_size` ≤ 0.01 m，精细物体 ≤ 0.005 m。

### Q4：想用自己的数据

1. 准备 `depth.png`（16-bit，毫米）、`mask.png`（二值）、`intrinsics.json`
2. 放入一个目录，运行：
   ```bash
   python pipeline_from_syllabus.py --demo-dir 你的目录 --no-vis
   ```

### Q5：使用 RealSense 相机实时采集

```bash
# 安装依赖
pip install pyrealsense2

# 运行 USB 模式
python interactive_depth_pipeline.py --source usb --output-dir output
```

---

## 附录：文件索引

| 文件 | 说明 |
|------|------|
| [`pipeline_from_syllabus.py`](pipeline_from_syllabus.py) | 课堂 5 步法独立脚本（本教程配套） |
| [`DepthMask_PointCloud_Pipeline.py`](DepthMask_PointCloud_Pipeline.py) | 深度图+掩膜→点云管道（含截图/拼图） |
| [`Open3D_PointCloud_Pipeline.py`](Open3D_PointCloud_Pipeline.py) | 核心函数库（反投影、滤波、可视化） |
| [`interactive_depth_pipeline.py`](interactive_depth_pipeline.py) | 交互式入口（整合输入源+SAM2+点云） |
| [`depth_sources.py`](depth_sources.py) | 深度数据输入源（文件/USB/网络） |
| [`interactive_mask_ui.py`](interactive_mask_ui.py) | OpenCV 交互掩膜选择 UI |
| [`sam2_segmenter.py`](sam2_segmenter.py) | SAM2 分割包装层 |
| [`PointCloud_Sandbox.py`](PointCloud_Sandbox.py) | 滤波参数实时调节沙盒 |

### 函数对应关系

| 课堂步骤 | 核心 API / 操作 | [`pipeline_from_syllabus.py`](pipeline_from_syllabus.py) 函数 |
|----------|----------------|------------------------------------------------|
| 1. 掩膜筛选 | `depth_mm * mask` | [`step1_mask_filtering()`](pipeline_from_syllabus.py:81) |
| 2. 反投影 | `create_from_depth_image()` | [`step2_backprojection()`](pipeline_from_syllabus.py:115) |
| 3. 直通滤波 | numpy 切片 `points[:, 2]` | [`step3_passthrough()`](pipeline_from_syllabus.py:201) |
| 4a. 统计滤波 | `remove_statistical_outlier()` | [`step4_denoise_and_downsample()`](pipeline_from_syllabus.py:254) |
| 4b. 体素降采样 | `voxel_down_sample()` | 同上 |
| 5. 导出 .pcd | `write_point_cloud()` | [`step5_export()`](pipeline_from_syllabus.py:338) |

---

> **下一讲预告**：利用本讲输出的干净点云 `syllabus_target_clean.pcd` 进行位姿估计，计算目标物体的 6D 位姿，为机械臂抓取提供空间参考。
