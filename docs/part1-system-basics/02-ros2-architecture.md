# 第 2 讲：机器人系统架构

## 2.1 本讲主线：从“单个模型”到“机器人系统”

### 2.1.1 为什么这一讲很重要？

假设一个策略模型已经计算出机械臂的下一步动作，机器人是否就一定能动起来？答案是否定的。

模型输出的动作可能与真实机械臂的关节顺序不一致，可能使用角度而驱动要求弧度，也可能超过关节限位。即使动作本身没有问题，机器人仍需要驱动节点与硬件通信，需要持续读取关节状态，需要控制器按照稳定频率执行，还要在碰撞、超时或急停触发时立即停止。任务结束后，系统还要判断是否成功，并把观测、动作和结果记录下来。

因此，具身智能不是“给机器人装上一个模型”，而是把本体、传感器、通信、策略、控制、安全和数据组织成一个持续运行的闭环系统。视觉—语言—动作模型（Vision-Language-Action，VLA）、世界模型、模仿学习和强化学习很重要，但它们通常只承担系统中的一部分职责。

可以把两种理解方式放在一起比较：

| 只看单个模型 | 观察完整机器人系统 |
| --- | --- |
| 关注输入能否映射为动作 | 关注观测、决策、执行和反馈能否持续闭环 |
| 默认输入数据已经准备好 | 追问图像、状态和目标由谁产生、是否同步 |
| 默认动作可以直接执行 | 检查单位、维度、限位、频率和安全条件 |
| 以一次推理结束为终点 | 以任务完成、失败恢复和数据记录为终点 |

本讲的作用，就是先建立这张系统地图。后续学习 VLA、ACT、Diffusion Policy、强化学习或世界模型时，读者才能判断模型应该接收什么信息、输出什么结果，以及如何与真实机器人连接。

### 2.1.2 本讲学习目标

学完本讲后，学生应能够：

1. 理解机器人系统为什么需要多个模块协同工作；
2. 理解机器人系统中本体层、控制层、感知层和策略层的关系；
3. 理解 ROS2 中 Node、Topic、Service、Action 和 Launch 的基本作用；
4. 判断 VLA、世界模型、模仿学习、强化学习在机器人系统中的位置；
5. 能够搭建一个最小机器人闭环：读取状态、发布目标、生成动作、执行控制、记录数据。

### 2.1.3 本讲整体逻辑

```text
机器人本体
↓
ROS2 驱动节点
↓
状态读取
↓
感知模块
↓
策略模型
↓
控制器
↓
动作执行
↓
任务反馈
↓
数据记录
```

这条链路中有两种方向相反的信息流。传感器数据和机器人状态从本体向上流动，告诉系统“现在发生了什么”；目标和控制指令从策略与控制模块向下流动，告诉机器人“下一步要做什么”。动作改变了机器人和环境，新的状态又回到系统中，于是闭环得以持续运行。

### 2.1.4 贯穿本讲的机械臂任务

为了避免每个概念都使用不同例子，本讲统一讨论一个最小任务：让一台六关节机械臂从当前位置移动到目标关节位置，并记录整个过程。

系统使用四个核心 Topic（话题）传递信息：

```text
目标发布：/target_joint
          ↓
当前状态：/joint_states → 策略节点 → /action_command
                                      ↓
                               控制与机器人本体
                                      ↓
任务反馈：                         /task_status
                                      ↓
                                episode 记录
```

`/target_joint` 表示目标关节位置，`/joint_states` 表示机器人当前关节状态，`/action_command` 表示策略产生并等待控制层处理的动作，`/task_status` 表示运行中、到达、超时或异常等任务状态。

真实机械臂可能使用不同 Topic 和消息类型。本讲采用这组名称，是为了让系统关系清晰、例子前后一致，而不是规定所有机械臂都必须使用相同接口。

### 2.1.5 本节小结

机器人任务的基本单位不是一次模型推理，而是一次包含感知、决策、控制、反馈和记录的闭环执行。本讲关注模块的职责、接口和数据流，不讨论某一种模型的训练细节。理解完整系统后，读者才能把后续算法放到正确的位置上。

## 2.2 机器人系统四层结构

为了理解复杂机器人系统，可以把它抽象为本体层、控制层、感知层和策略层。四层结构不是唯一的工程划分，也不意味着每一层只能运行一个进程。它是一张职责地图：当系统出现问题时，我们可以先判断问题属于“身体和传感器”“动作执行”“环境理解”，还是“任务决策”。

### 2.2.1 本体层

本体层是机器人与物理世界直接接触的部分，包括机械臂、夹爪、移动底盘、人形身体等执行机构，也包括相机、编码器、惯性测量单元（IMU）、力传感器和触觉传感器等感知硬件。

本体层决定了机器人“能做什么”和“能感受到什么”。六轴机械臂可以在工作空间中调整末端位置与姿态，但不能像移动底盘一样跨房间移动；普通夹爪可以完成开合抓取，却无法提供灵巧手那样的多指操作能力。相机提供环境图像，编码器则提供关节角度，两者都是上层模块作出判断的依据。

在本章案例中，机械臂与关节编码器属于本体层。它接收经过控制层处理的指令，产生真实运动，再通过编码器输出新的关节状态。这里最重要的边界是：本体层执行的是驱动能够理解的命令，而不是自然语言，也不一定是策略模型的原始输出。

### 2.2.2 控制层

控制层负责把“想做什么”转换为“机器人怎样稳定地做到”。常见能力包括关节位置控制、速度控制、末端位姿控制、夹爪控制、轨迹插值、动作平滑、限幅和急停处理。

例如，策略层给出“关节 2 向正方向移动 0.1 弧度”，控制层不会只把数字原样转发。它要检查目标是否超出关节范围，限制单个控制周期内的最大变化量，并按照固定频率将命令下发给驱动。如果状态反馈中断或通信超时，控制层还应停止继续执行。

本章的 `/action_command` 是策略层与控制层之间的教学接口。它表达“建议执行的动作”，不是可以绕过厂商驱动和安全控制直接发送给电机的命令。真实系统还可能使用轨迹控制器、阻抗控制器或厂商提供的实时接口。

### 2.2.3 感知层

感知层把传感器产生的原始数据转换为可以支持行动的信息。相机节点发布图像后，感知模块可以继续执行目标检测、图像分割、深度估计、点云处理和目标位姿估计；机器人自身的关节角、末端位姿和接触状态也属于广义的状态感知。

“看见一个杯子”还不是足够的机器人感知结果。系统通常还需要知道杯子在哪里、是否被遮挡、从哪个方向接近，以及坐标如何从相机系转换到机器人基座系。感知层的输出会被策略、规划或控制模块消费。

本章的最小关节任务不依赖相机，主要使用 `/joint_states` 作为机器人自身状态。这样读者可以先理解闭环通信；扩展到抓取任务时，再把相机图像、目标检测和位姿估计接入同一系统。

### 2.2.4 策略层

策略层负责回答“下一步应该做什么”。它可能是几行手写规则，也可能是传统规划器、模仿学习策略、强化学习策略、VLA 模型或高层任务规划模块。策略的输入通常包括任务目标、环境观测和机器人状态，输出可以是高层子目标、目标位姿、单步动作或一段动作序列。

在本章案例中，`policy_node` 读取 `/target_joint` 和 `/joint_states`，计算目标与当前位置之间的误差，并发布 `/action_command`。这个简单比例策略并不智能，却能把策略层的输入、输出和职责边界展示清楚。之后即使把它替换成 ACT 或 Diffusion Policy，周围的状态读取、控制、安全与记录模块仍然需要保留。

### 2.2.5 四层结构总览

| 层级 | 核心问题 | 典型输入 | 典型输出 | 本章案例 |
| --- | --- | --- | --- | --- |
| 策略层 | 下一步做什么 | 任务、观测、机器人状态 | 目标或动作 | 由目标误差生成 `/action_command` |
| 感知层 | 环境和机器人现在怎样 | 图像、深度、传感器原始数据 | 目标、位姿、语义与状态 | 读取 `/joint_states`；抓取扩展中识别目标 |
| 控制层 | 动作怎样安全稳定地执行 | 目标、动作、当前状态 | 驱动可执行的控制指令 | 检查并执行 `/action_command` |
| 本体层 | 系统能感知和作用什么 | 电机指令、物理环境 | 运动、关节和传感器反馈 | 机械臂运动并产生关节反馈 |

从数据方向看，状态通常由本体层向上流动，动作则由策略层向下流动：

```text
任务与目标
    ↓
策略层：生成目标或动作
    ↓
控制层：适配、限幅和稳定执行
    ↓
本体层：在物理世界中运动
    ↑
感知层：读取环境与机器人状态
    └────────── 反馈给策略与控制 ──────────┘
```

真实工程中的边界可能交叉。例如，强化学习可以直接输出低层控制量，控制器也会读取力传感器形成局部反馈。四层结构用于澄清主要职责，而不是限制软件必须怎样拆分。

### 2.2.6 配图建议

如果后续补充课程插图，可绘制“机器人系统四层架构图”：以 LeRobot / SO101 为例，从下到上展示本体层、控制层、感知层和策略层，左侧标注传感器与状态数据上行，右侧标注目标和控制指令下行。当前文字流程图已经包含完整信息，不依赖外部图片也能阅读。

### 2.2.7 本节小结

四层架构把机器人系统分为身体、执行、理解和决策四类主要职责。一个稳定系统不仅要让各层分别工作，还要定义清晰的输入输出，使状态能够及时上行、动作能够安全下行、异常能够被发现并处理。

## 2.3 ROS2 基础：机器人模块如何通信？

### 2.3.1 ROS2 在机器人系统中的作用

ROS2 是 Robot Operating System 2 的简称。虽然名称中有“操作系统”，它并不是 Linux 或 Windows 那样直接管理计算机硬件的操作系统，而是一套用于开发机器人软件的通信机制、工具和工程组织方式。

它帮助开发者把相机、状态读取、策略、控制和数据记录拆成相对独立的模块，再通过标准消息连接起来。这样做有三个直接好处：一个模块可以单独启动和调试；相同接口可以替换不同实现；系统运行时可以观察节点、消息和数据频率，而不必把所有逻辑塞进一个大程序。

一个最小 ROS2 工程通常涉及三个基本概念：

- **工作空间（workspace）**：存放一个或多个 ROS2 Package，并用于统一构建；
- **功能包（Package）**：组织某一组相关节点、配置、Launch 文件和依赖；
- **节点（Node）**：真正运行并承担一项功能的进程内单元。

工作空间像课程项目文件夹，Package 像其中一个功能模块，Node 则像实际工作的成员。节点之间主要通过 Topic、Service 和 Action 通信，Launch 用于把整套系统一次启动起来。

### 2.3.2 Node

Node（节点）是 ROS2 中的基本运行单元。一个节点通常承担一个边界清晰的功能，例如读取相机、发布机器人状态、运行策略或记录数据。节点可以位于同一台计算机，也可以通过网络分布在不同计算机上。

本章 Demo 使用以下节点：

| 节点 | 主要职责 |
| --- | --- |
| `target_publisher` | 发布目标关节位置 |
| `robot_state_node` | 从 mock 对象或硬件读取状态，并发布 `/joint_states` |
| `policy_node` | 根据目标和当前状态生成 `/action_command` |
| `controller_node` | 在 mock 环境中更新模拟对象，或适配真实硬件控制接口 |
| `task_status_node` | 判断任务处于运行、到达、超时还是异常状态 |
| `episode_recorder` | 记录目标、状态、动作、时间戳和任务结果 |

“一个节点负责一个清晰功能”是设计建议，而不是强制规则。简单 Demo 可以把多个功能写在一个节点中；大型系统也可能把一个复杂功能拆成多个节点。关键是输入、输出和故障边界要容易理解。

启动系统后，可以查看当前发现的节点：

```bash
ros2 node list
```

如果某个预期节点没有出现，应先检查它是否成功启动，而不是立即怀疑算法本身。

### 2.3.3 Topic

Topic（话题）适合持续或事件式的数据流。发布者把消息发送到一个 Topic，订阅者接收该 Topic 的消息；两者不需要直接调用对方，也不要求同时写在同一个程序里。

相机图像、关节状态、目标位置、动作指令和任务状态都适合通过 Topic 传递。本章使用：

- `/target_joint`：目标关节位置；
- `/joint_states`：当前关节名称、位置、速度和力矩等状态；
- `/action_command`：策略节点生成的动作；
- `/task_status`：任务的当前状态。

下面的片段展示 `target_publisher` 如何创建发布者并发送一个六关节目标。队列深度 `10` 表示通信层最多保留一定数量尚未处理的消息；真实系统还要根据数据的重要性和实时性配置服务质量（Quality of Service，QoS）。

```python
from std_msgs.msg import Float64MultiArray

self.target_pub = self.create_publisher(
    Float64MultiArray, "/target_joint", 10
)

msg = Float64MultiArray()
msg.data = [0.0, -0.4, 0.8, 0.0, 0.4, 0.0]
self.target_pub.publish(msg)
```

调试 Topic 时，常用命令包括：

```bash
ros2 topic list
ros2 topic echo /joint_states
ros2 topic hz /joint_states
```

第一条列出当前 Topic，第二条查看消息内容，第三条测量发布频率。Topic 名称存在并不代表数据一定正常，还要继续检查消息类型、内容和频率。

### 2.3.4 Service

Service（服务）适合“发出一次请求，等待一次响应”的短操作。它更像一次函数调用：客户端发送请求，服务端处理后返回结果。

例如，复位系统、查询一次设备状态、切换控制模式或保存当前配置都可以使用 Service。调用者通常希望很快获得明确结果：操作成功、失败，或返回某个查询值。

Service 不适合持续发布相机图像，也不适合需要几十秒完成且需要中途反馈的机械臂轨迹。前者应该使用 Topic，后者更适合 Action。本章的最小闭环主要使用 Topic；如果要增加“一次性保存当前 episode”，可以设计 `/save_episode` Service。

### 2.3.5 Action

Action（动作接口）适合需要一段时间完成的任务，例如导航到目标位置、执行一段轨迹或完成一次抓取。客户端发送目标后，服务端可以持续返回进度，客户端也可以取消任务，最后再获得成功、失败或被取消等结果。

假设机械臂移动到目标位置需要 5 秒。如果使用 Service，调用者通常只能等待最终响应，很难获得中途进度或安全取消；使用 Action，则可以持续报告“已完成 40%”或“当前误差为 0.08 弧度”，并在发现障碍时取消任务。

本章为了突出底层数据流，使用 Topic 和 `/task_status` 展示闭环。工程中也可以把完整“移动到目标关节位置”封装成 Action，在 Action 服务端内部继续使用状态 Topic、控制器和安全检查。

### 2.3.6 Launch

Launch 用于统一描述和启动多个节点。除了减少逐个打开终端的工作，它还可以设置参数、命名空间、重映射规则、条件和节点启动方式。

本章 Demo 包含六个节点。如果逐个手动启动，很容易漏掉 `episode_recorder`，也不利于复现实验。Launch 文件可以把状态、目标、策略、控制、任务判断和记录节点组织成一套系统。读者只需执行一次 Launch 命令，就能得到一致的启动配置。

需要注意，Launch 负责“怎样启动”，并不自动保证节点之间的数据语义正确。Topic 名称、消息类型、单位和频率仍然需要设计与验证。

### 2.3.7 Topic / Service / Action 对比表

| 通信方式 | 交互形式 | 是否持续 | 过程反馈 | 能否取消 | 典型机器人场景 |
| --- | --- | --- | --- | --- | --- |
| Topic | 发布—订阅 | 可以持续 | 由消息流本身提供 | 不适用 | 图像、关节状态、动作指令 |
| Service | 一次请求—一次响应 | 通常很短 | 通常没有 | 通常不支持 | 复位、查询状态、切换模式 |
| Action | 发送目标—反馈—结果 | 可以持续较长时间 | 支持 | 支持 | 导航、轨迹、抓取任务 |

选择通信方式时，可以先问三个问题：数据是否持续产生？调用者是否必须等待明确响应？任务是否需要进度和取消？连续数据优先考虑 Topic，短请求优先考虑 Service，长任务优先考虑 Action。

### 2.3.8 ROS2 通信的边界

ROS2 解决的是模块发现、消息传输和工程组织问题，并不会自动解决感知误差、控制稳定性或模型泛化。即使所有 Topic 都有数据，机器人仍可能因为单位错误、时间不同步或动作越界而失败。因此，通信“连通”只是系统正确性的第一步。

### 2.3.9 本节小结

Node 把系统拆成职责清晰的运行单元；Topic 承载持续数据，Service 处理短请求，Action 管理长任务，Launch 负责统一启动。理解这些概念的重点不是记住定义，而是能根据数据和任务特征选择合适的通信方式。

## 2.4 具身智能前沿模型在系统中的位置

建立四层结构和 ROS2 通信概念后，就可以回答一个常见问题：VLA、Diffusion Policy 或世界模型应该放在哪里？判断方法不是看模型名称，而是看它消费什么信息、产生什么结果，以及谁负责验证并执行这个结果。

### 2.4.1 VLM / 检测 / 分割模型

视觉—语言模型（Vision-Language Model，VLM）、目标检测和图像分割模型主要位于感知层。它们读取图像或图像与文本的组合，输出类别、边界框（bounding box）、分割掩码（mask）或语义描述。

这些输出通常还不能直接用于控制机械臂。例如，二维图像中的边界框不能直接告诉机械臂目标在基座坐标系中的三维位置。系统还可能需要深度信息、相机标定、坐标变换和目标位姿估计。感知模型回答“目标是什么、在哪里”，策略与控制模块再决定“怎样行动”。

### 2.4.2 VLA 模型

视觉—语言—动作模型（Vision-Language-Action，VLA）主要位于策略层。它通常接收语言指令、图像观测和机器人状态，输出单步动作、动作序列或中间目标。

VLA 的价值在于把“看见场景、理解指令、生成动作”连接起来。但模型输出动作后，系统仍要完成动作空间映射、关节顺序与单位适配、控制频率管理、限幅和安全检查。VLA 可以替换本章中简单的 `policy_node` 逻辑，却不能替代机械臂驱动、状态发布、底层控制和急停保护。

### 2.4.3 ACT / Diffusion Policy

ACT（Action Chunking with Transformers）与 Diffusion Policy 常用于模仿学习策略。它们根据图像和机器人状态生成动作，既可以预测下一步，也可以一次输出一段 action chunk（动作块）。动作块能减少高层推理次数，并利用时间上下文生成更连贯的动作。

一次输出多步动作也带来新的系统问题：执行过程中环境可能变化，旧动作是否还有效？系统应该以什么频率重新推理？动作块如何与最新反馈融合？因此，策略节点需要与执行队列、反馈监测和安全模块协同，而不是把整段动作无条件执行到底。

### 2.4.4 强化学习

强化学习可以位于策略层，也可以深入控制层。高层策略可以学习选择子目标或操作技能；低层策略则可能直接输出关节目标、力矩或足端动作，常见于四足运动、人形机器人平衡、灵巧操作和仿真控制。

输出越接近低层电机命令，对实时性、稳定性和安全性的要求越高。仿真中有效的策略还会面临仿真到真实（sim-to-real）差异。因此，真实部署前需要完成仿真评测、动作范围检查、低速测试和保护机制验证。

### 2.4.5 世界模型

世界模型学习“当前状态、动作和未来状态之间的关系”。它可以预测执行某个动作后环境怎样变化，帮助策略比较候选动作、规划更长时序任务，或生成用于训练和评估的数据。

世界模型的预测不是物理世界本身。预测时间越长，误差越可能累积，所以系统仍要用真实传感器反馈不断校正。工程上，世界模型通常与策略或规划模块协作，不直接取代状态读取和闭环控制。

### 2.4.6 数据飞轮

数据飞轮贯穿整个系统，而不只属于某一层。机器人执行任务时产生图像、状态、动作、人工干预和任务结果；系统把这些数据记录为 episode，再进行清洗、标注、训练、评测和重新部署。

完整循环可以表示为：

```text
任务执行 → 数据记录 → 成功与失败筛选 → 清洗和标注
    ↑                                      ↓
重新部署 ← 模型评测 ← 模型训练 ← 训练样本构建
```

失败数据尤其重要。只有记录“看到了什么、执行了什么、何时失败、为什么失败”，团队才能判断问题来自感知、策略、控制还是硬件，并为下一轮训练补充针对性数据。

### 2.4.7 模型位置总览表

| 模型或方法 | 主要位置 | 典型输入 | 典型输出 | 接入系统时还需要什么 |
| --- | --- | --- | --- | --- |
| VLM / 检测 / 分割 | 感知层 | 图像、文本 | 类别、框、mask、语义 | 位姿估计与坐标变换 |
| VLA | 策略层 | 语言、图像、状态 | 动作或动作序列 | 动作适配、控制器、安全检查 |
| ACT / Diffusion Policy | 策略层 | 观测、状态 | action chunk | 时序执行、限幅、反馈 |
| 强化学习 | 策略层或控制层 | 状态、观测 | 高层动作或低层控制量 | 仿真验证与真实机安全约束 |
| 世界模型 | 预测与规划模块 | 历史状态、观测、动作 | 未来预测或候选轨迹 | 规划器、策略与真实反馈校正 |

表中“主要位置”表示典型职责，不是不可改变的固定归属。无论使用哪种模型，都应明确输入数据由谁产生、输出接口由谁消费、异常由谁处理，以及任务结果由谁记录。

### 2.4.8 本节小结

前沿模型只有接入完整系统才能在物理世界中发挥作用。感知模型提供可行动的信息，策略模型生成目标或动作，预测模型辅助规划，控制与安全模块保证动作可执行，数据系统记录结果并支持下一轮改进。

## 2.5 机器人系统闭环：从状态读取到数据记录

### 2.5.1 最小机器人闭环

```text
读取状态
↓
发布目标
↓
生成动作
↓
下发控制
↓
执行运动
↓
读取反馈
↓
判断结果
↓
记录 episode
```

### 2.5.2 状态读取

包括：
- 关节位置；
- 关节速度；
- 夹爪状态；
- 相机图像；
- 深度图；
- 目标点；
- 任务状态。

### 2.5.3 动作生成

动作可以来自：
- 手写规则；
- 简单控制策略；
- 模仿学习模型；
- VLA 模型；
- 强化学习策略；
- 世界模型辅助规划。

### 2.5.4 动作执行

执行前需要检查：
- 动作范围；
- 速度限制；
- 关节限位；
- 碰撞风险；
- 控制频率；
- 夹爪状态；
- 急停条件。

### 2.5.5 任务反馈

反馈信息包括：
- 是否到达目标；
- 是否抓取成功；
- 是否发生碰撞；
- 是否超时；
- 是否丢失目标；
- 是否需要重试。

### 2.5.6 Episode 数据记录

一个 episode 通常包括：
- 图像；
- 状态；
- action；
- 目标；
- 时间戳；
- 任务结果；
- 成功 / 失败标签；
- 异常信息。

## 2.6 Demo 设计：基于 ROS2 的 LeRobot / SO101 最小闭环

### 2.6.1 Demo 目标

```text
完成一个基于 ROS2 的最小机器人闭环：
读取 joint_states
↓
发布 target_pose 或 target_joint
↓
policy_node 生成 action_command
↓
controller_node 下发动作
↓
读取反馈状态
↓
判断是否到达目标
↓
保存 episode 数据
```

### 2.6.2 有硬件版 Demo

Demo 名称：
- 基于 ROS2 的 LeRobot / SO101 机械臂状态读取与动作下发闭环。

适合设备：
- SO101；
- LeRobot 兼容机械臂；
- ROS2；
- 相机；
- 夹爪；
- 数据记录模块。

流程：
1. 连接 LeRobot / SO101 机械臂；
2. 启动 ROS2 驱动节点；
3. 读取当前 /joint_states；
4. 发布 /target_pose 或 /target_joint；
5. 控制节点生成 /action_command；
6. 下发机械臂动作；
7. 读取反馈状态；
8. 判断是否到达目标；
9. 记录图像、状态、动作和结果；
10. 保存为 episode_0001。

输入示例：
target_eef = [0.20, 0.05, 0.12]
输出示例：
/joint_states
/target_pose
/action_command
/gripper_command
/task_status
episode_0001

### 2.6.3 无硬件仿真版 Demo

Demo 名称：
- 基于 ROS2 的 LeRobot 机械臂仿真控制闭环。

可选环境：
- ROS2 mock robot；
- MuJoCo 简化机械臂；
- ManiSkill reaching task；
- Isaac Lab 机械臂环境。

设计节点：
- camera_node；
- robot_state_node；
- target_publisher；
- policy_node；
- controller_node；
- task_status_node；
- episode_recorder。

Topic：
- /camera/image；
- /joint_states；
- /target_pose；
- /action_command；
- /gripper_command；
- /task_status；
- /episode_status。

流程：
1. target_publisher 发布目标点；
2. robot_state_node 发布当前关节状态；
3. policy_node 根据目标和状态生成动作；
4. controller_node 下发动作；
5. task_status_node 判断任务是否完成；
6. episode_recorder 记录图像、状态、动作和结果。

### 2.6.4 Demo 融入的知识点

> 待补充：该图表内容需从原飞书文档或源素材补齐。

## 2.7 实验步骤

### 2.7.1 环境准备

1. 创建 ROS2 workspace；
2. 创建 LeRobot demo package；
3. 配置依赖和消息类型；
4. 准备硬件或仿真环境。

### 2.7.2 节点编写

1. 编写 robot_state_node，发布关节状态；
2. 编写 target_publisher，发布目标点；
3. 编写 policy_node，生成简单动作指令；
4. 编写 controller_node，下发动作；
5. 编写 task_status_node，判断是否完成；
6. 编写 episode_recorder，记录图像、状态、动作和任务结果。

### 2.7.3 系统调试

1. 使用 ros2 topic list 查看 Topic；
2. 使用 ros2 topic echo 查看消息内容；
3. 使用 rqt_graph 查看节点连接；
4. 修改目标点，观察系统响应；
5. 修改控制参数，观察闭环稳定性；
6. 保存并检查 episode 数据。

## 2.8 作业交付与失败复盘

### 2.8.1 作业交付

学生需要提交：
- ROS2 package 源码；
- rqt_graph 截图；
- Topic / Service / Action 使用说明；
- 一张 LeRobot 机械臂数据流图；
- 一段硬件或仿真闭环运行视频；
- 一个 episode 数据样例；
- 一段说明：VLA、世界模型、控制器分别位于机器人系统的哪个模块。

### 2.8.2 常见失败 checklist

1. Topic 名称是否一致？
2. 消息类型是否匹配？
3. 节点是否全部启动？
4. Topic 是否真的有数据发布？
5. 控制频率是否太低？
6. 状态更新是否滞后？
7. 动作下发后是否有反馈？
8. 目标点坐标系是否正确？
9. 是否只记录动作，没有记录观测和任务结果？
10. 策略输出动作是否经过安全限制？
11. episode 是否包含图像、状态、动作、时间戳和结果？
12. rqt_graph 中节点连接是否符合预期？

### 2.8.3 复盘问题

- 为什么机器人任务需要持续反馈？
- Topic 和 Service 分别适合什么场景？
- 为什么长任务更适合 Action？
- 为什么 LeRobot 数据不能只记录动作？
- 为什么 VLA 需要接入状态、控制和安全模块？
- 为什么失败样本比成功样本更有迭代价值？

## 2.9 参考开源项目

LeRobot
https://github.com/huggingface/lerobot
LeRobot 官方资源页
https://huggingface.co/lerobot
LeRobot ROS
https://github.com/ycheng517/lerobot-ros
SO101 ROS2
https://github.com/msf4-0/so101_ros2
SO101 ROS Physical AI
https://github.com/legalaspro/so101-ros-physical-ai
ROS2 examples
https://github.com/ros2/examples
ROS2 demos
https://github.com/ros2/demos
